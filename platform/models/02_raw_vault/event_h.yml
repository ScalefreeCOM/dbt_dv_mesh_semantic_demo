version: 2

models:
  - name: event_h
    description: "Standard hub for event_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_event_h
        description: "Hash key for event_h"
        data_tests:
          - unique
          - not_null
      - name: event_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
