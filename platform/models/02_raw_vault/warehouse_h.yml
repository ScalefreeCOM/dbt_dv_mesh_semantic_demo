version: 2

models:
  - name: warehouse_h
    description: "Standard hub for warehouse_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_warehouse_h
        description: "Hash key for warehouse_h"
        data_tests:
          - unique
          - not_null
      - name: warehouse_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
