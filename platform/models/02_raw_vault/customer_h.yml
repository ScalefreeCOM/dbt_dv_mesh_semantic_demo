version: 2

models:
  - name: customer_h
    description: "Standard hub for customer_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_customer_h
        description: "Hash key for customer_h"
        data_tests:
          - unique
          - not_null
      - name: customer_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
