version: 2

models:
  - name: warehouse_inventory_n_s_v0
    description: "Standard satellite for warehouse_h"
    config:
      tags: ['satellite', 'standard', 'raw_vault']
    columns:
      - name: hk_warehouse_h
        description: "Parent entity hash key"
        data_tests:
          - not_null
      - name: hd_warehouse_inventory_n_s
        description: "Hash diff for change detection"
      - name: warehouse_name
        description: "Payload column"
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
