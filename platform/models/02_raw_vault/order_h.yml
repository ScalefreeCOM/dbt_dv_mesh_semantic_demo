version: 2

models:
  - name: order_h
    description: "Standard hub for order_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_order_h
        description: "Hash key for order_h"
        data_tests:
          - unique
          - not_null
      - name: order_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
