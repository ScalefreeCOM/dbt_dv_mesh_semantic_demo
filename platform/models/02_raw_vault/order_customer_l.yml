version: 2

models:
  - name: order_customer_l
    description: "Standard link for order_customer_l"
    config:
      tags: ['link', 'raw_vault']
    columns:
      - name: hk_order_customer_l
        description: "Link hash key"
        data_tests:
          - unique
          - not_null
      - name: hk_order_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_customer_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
