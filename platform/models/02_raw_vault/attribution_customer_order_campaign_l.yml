version: 2

models:
  - name: attribution_customer_order_campaign_l
    description: "Standard link for attribution_customer_order_campaign_l"
    config:
      tags: ['link', 'raw_vault']
    columns:
      - name: hk_attribution_customer_order_campaign_l
        description: "Link hash key"
        data_tests:
          - unique
          - not_null
      - name: hk_attribution_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_customer_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_order_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_campaign_attributed_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_campaign_first_touch_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_campaign_last_touch_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
