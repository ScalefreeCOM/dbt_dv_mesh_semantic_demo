version: 2

models:
  - name: event_product_l
    description: "Standard link for event_product_l"
    config:
      tags: ['link', 'raw_vault']
    columns:
      - name: hk_event_product_l
        description: "Link hash key"
        data_tests:
          - unique
          - not_null
      - name: hk_event_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: hk_product_h
        description: "Foreign key to hub"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
