version: 2

models:
  - name: campaign_h
    description: "Standard hub for campaign_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_campaign_h
        description: "Hash key for campaign_h"
        data_tests:
          - unique
          - not_null
      - name: campaign_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
