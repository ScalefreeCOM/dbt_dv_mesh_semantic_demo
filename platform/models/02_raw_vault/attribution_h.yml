version: 2

models:
  - name: attribution_h
    description: "Standard hub for attribution_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_attribution_h
        description: "Hash key for attribution_h"
        data_tests:
          - unique
          - not_null
      - name: attribution_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
