version: 2

models:
  - name: payment_pgw_n_s_v1
    description: "Standard satellite v1 (with load_end_date) for payment_h"
    config:
      tags: ['satellite', 'v1', 'raw_vault']
    columns:
      - name: hk_payment_h
        description: "Parent entity hash key"
        data_tests:
          - not_null
      - name: hd_payment_pgw_n_s
        description: "Hash diff for change detection"
      - name: payment_date
        description: "Payload column"
      - name: payment_method
        description: "Payload column"
      - name: amount
        description: "Payload column"
      - name: currency
        description: "Payload column"
      - name: payment_status
        description: "Payload column"
      - name: gateway
        description: "Payload column"
      - name: ldts
        description: "Load date timestamp"
      - name: load_end_date
        description: "Load end date (when this version was superseded)"
      - name: is_current
        description: "Flag indicating if this is the current version"
