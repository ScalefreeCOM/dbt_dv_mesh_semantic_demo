version: 2

models:
  - name: inventory_movement_h
    description: "Standard hub for inventory_movement_h"
    config:
      tags: ['hub', 'raw_vault']
    columns:
      - name: hk_inventory_movement_h
        description: "Hash key for inventory_movement_h"
        data_tests:
          - unique
          - not_null
      - name: movement_id
        description: "Business key column"
        data_tests:
          - not_null
      - name: ldts
        description: "Load date timestamp"
      - name: rsrc
        description: "Record source"
