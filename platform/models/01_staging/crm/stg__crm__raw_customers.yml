version: 2

models:
  - name: stg__crm__raw_customers
    description: "Staging model for raw_customers from CRM"
    config:
      tags: ['staging', 'crm']
    columns:
      - name: rsrc
        description: "Record source identifier"
      - name: ldts
        description: "Load date timestamp"
      - name: hk_customer_h
        description: "Hash key"
      - name: hd_customer_crm_n_s
        description: "Hash diff for customer_crm_n_s"
      - name: hd_customer_crm_p_s
        description: "Hash diff for customer_crm_p_s"
