version: 2

models:
  - name: stg__pim__raw_products
    description: "Staging model for raw_products from PIM"
    config:
      tags: ['staging', 'pim']
    columns:
      - name: rsrc
        description: "Record source identifier"
      - name: ldts
        description: "Load date timestamp"
      - name: hk_product_h
        description: "Hash key"
      - name: hd_product_pim_n_s
        description: "Hash diff for product_pim_n_s"
