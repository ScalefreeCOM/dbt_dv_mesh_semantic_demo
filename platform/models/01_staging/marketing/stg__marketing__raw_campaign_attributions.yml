version: 2

models:
  - name: stg__marketing__raw_campaign_attributions
    description: "Staging model for raw_campaign_attributions from Marketing"
    config:
      tags: ['staging', 'marketing']
    columns:
      - name: rsrc
        description: "Record source identifier"
      - name: ldts
        description: "Load date timestamp"
      - name: hk_attribution_h
        description: "Hash key"
      - name: hd_attribution_marketing_n_s
        description: "Hash diff for attribution_marketing_n_s"
