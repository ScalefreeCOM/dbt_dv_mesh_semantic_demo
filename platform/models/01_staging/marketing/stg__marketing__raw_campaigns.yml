version: 2

models:
  - name: stg__marketing__raw_campaigns
    description: "Staging model for raw_campaigns from Marketing"
    config:
      tags: ['staging', 'marketing']
    columns:
      - name: rsrc
        description: "Record source identifier"
      - name: ldts
        description: "Load date timestamp"
      - name: hk_campaign_h
        description: "Hash key"
      - name: hd_campaign_marketing_n_s
        description: "Hash diff for campaign_marketing_n_s"
