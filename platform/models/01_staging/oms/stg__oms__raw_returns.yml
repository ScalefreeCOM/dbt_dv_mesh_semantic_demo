version: 2

models:
  - name: stg__oms__raw_returns
    description: "Staging model for raw_returns from OMS"
    config:
      tags: ['staging', 'oms']
    columns:
      - name: rsrc
        description: "Record source identifier"
      - name: ldts
        description: "Load date timestamp"
      - name: hk_return_h
        description: "Hash key"
      - name: hd_return_oms_n_s
        description: "Hash diff for return_oms_n_s"
